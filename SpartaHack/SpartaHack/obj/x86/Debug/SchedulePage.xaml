<Page
    x:Class="SpartaHack.SchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpartaHack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="TargetBackground" Color="#FF030316"/>
            <SolidColorBrush x:Key="TargetForeground" Color="#FF80FFDB"/>
            <FontFamily x:Key="TargetFont">/Assets/Moondance.otf#Moondance</FontFamily>
            <CollectionViewSource x:ConnectionId='1' x:Name="Events" IsSourceGrouped="True"/>
            <DataTemplate x:ConnectionId='2' x:Name="tmpEvent">
                <StackPanel Margin="0,5">
                    
                    <TextBlock  FontSize="22" VerticalAlignment="Bottom"  Text="{Binding Title}" Foreground="{StaticResource TargetForeground}" />
                    <TextBlock  FontSize="12" Margin="0,5,0,0" TextWrapping="WrapWholeWords" Text="{Binding Time}" Foreground="White"  FontWeight="Bold"/>

                    <TextBlock  FontSize="12" Margin="5,5,0,0" TextWrapping="WrapWholeWords" Text="{Binding Description}" Foreground="White"/>
                
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:ConnectionId='3' x:Name="lsvHeader">
                <TextBlock FontSize="18" Foreground="{StaticResource TargetForeground}" Text="{Binding Day}" FontFamily="{StaticResource TargetFont}"/>
            </DataTemplate>
        </ResourceDictionary>

    </Page.Resources>
    <Grid  Background="{StaticResource TargetBackground}" >
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:ConnectionId='4' x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="700" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                        <Setter Target="TopAppBar.Visibility"
                            Value="Visible" />
                        <Setter Target="BottomAppBar.Visibility"
                            Value="Collapsed" />

                    </VisualState.Setters>
                </VisualState>

                <VisualState x:ConnectionId='5' x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />

                    </VisualState.StateTriggers>

                    <VisualState.Setters>


                        <Setter Target="TopAppBar.Visibility"
                            Value="Collapsed" />
                        <Setter Target="BottomAppBar.Visibility"
                            Value="Visible" />



                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>



        <ListView  IsSynchronizedWithCurrentItem="False" ItemsSource="{Binding Source={StaticResource Events}}" ItemTemplate="{StaticResource tmpEvent}">
            <ListView.GroupStyle>
                <GroupStyle HeaderTemplate="{Binding Source={StaticResource lsvHeader}}"/>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
    <Page.TopAppBar>
        <CommandBar x:ConnectionId='6' Margin="48,48,0,0" x:Name="TopAppBar" Background="{StaticResource TargetBackground}" Visibility="Collapsed">
            <AppBarButton x:ConnectionId='7' Icon="Refresh"                          />
            
        </CommandBar>
    </Page.TopAppBar>
    <Page.BottomAppBar>
        <CommandBar x:ConnectionId='8' x:Name="BottomAppBar" Background="{StaticResource TargetBackground}" Visibility="Collapsed" >
            <AppBarButton x:ConnectionId='9' Icon="Refresh"                          />
        </CommandBar>
    </Page.BottomAppBar>
</Page>

